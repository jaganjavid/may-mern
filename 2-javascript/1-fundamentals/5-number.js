

const num1 = 100;
const num2 = 50;

let val;

// Simple math with number

val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

// Math Object .

val = Math.PI; // Property
val = Math.round(2.5); // Method
val = Math.ceil(2.1); // Upper Number
val = Math.floor(2.5); // Lower Number
val = Math.sqrt(64);
val = Math.abs(-5); // always return the positive value
val = Math.min(-5, 0,199,3,6,7,);
val = Math.max(-5, 0, 3,6,7,199);
val = Math.random();

val = Math.floor(Math.random() * 10000);

console.log(val);